# Synthetic audit fixture: AWS secret access key in the camelCase keyword
# form the SSM agent log can echo. The detector value is length-anchored
# (35-45 chars), so short synthetic literals such as SecretAccessKey=EXAMPLE
# in the Windows-tier tests cannot false-positive. Synthetic value.
SecretAccessKey=ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmn
