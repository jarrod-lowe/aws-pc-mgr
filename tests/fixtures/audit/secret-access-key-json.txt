# Synthetic audit fixture: AWS secret access key in the JSON-style colon
# form (quoted key, colon separator, quoted value), as registration payloads,
# credential-file dumps and structured log echoes print it. The detector
# value is length-anchored (35-45 chars), so short synthetic literals such
# as SecretAccessKey=EXAMPLE in the Windows-tier tests cannot
# false-positive. Synthetic value.
  "SecretAccessKey": "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmn"
