# Credentials and region come from the environment's normal AWS SDK
# credential resolution chain (e.g. an externally selected AWS_PROFILE).
# Nothing user- or environment-specific is configured here.
provider "aws" {
}
